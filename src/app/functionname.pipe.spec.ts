import { FunctionnamePipe } from './functionname.pipe';

describe('FunctionnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FunctionnamePipe();
    expect(pipe).toBeTruthy();
  });
});
