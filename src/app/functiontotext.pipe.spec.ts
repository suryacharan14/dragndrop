import { FunctiontotextPipe } from './functiontotext.pipe';

describe('FunctiontotextPipe', () => {
  it('create an instance', () => {
    const pipe = new FunctiontotextPipe();
    expect(pipe).toBeTruthy();
  });
});
